import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import { TooltipProvider } from "@/components/ui/tooltip";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Zyneth - One-Click Exposure to Crypto Narratives",
  description:
    "Deposit into curated on-chain narrative vaults. Get diversified sector exposure in one transaction. No manual rebalancing. No complexity.",
  openGraph: {
    title: "Zyneth - One-Click Exposure to Crypto Narratives",
    description:
      "Deposit into curated on-chain narrative vaults. Get diversified sector exposure in one transaction.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        <TooltipProvider>{children}</TooltipProvider>
      </body>
    </html>
  );
}
